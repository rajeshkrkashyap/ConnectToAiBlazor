<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ConnectToAi.MobileApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ConnectToAi.MobileApp"
    xmlns:view="clr-namespace:ConnectToAi.MobileApp.Views"
    Shell.FlyoutBehavior="Disabled"
    Shell.NavBarIsVisible="False"
    x:Name="mainShell">
    <ShellContent 
        x:Name="loginPage" 
        Route="login"
        ContentTemplate="{DataTemplate local:Login}"/>

    <!--<FlyoutItem Title="Home" FlyoutDisplayOptions="AsMultipleItems">
        <ShellContent 
        x:Name="loginPage" 
        Route="login"
        ContentTemplate="{DataTemplate local:Login}"/>

        <ShellContent
        x:Name="homePage"    
        Title="Home"
        Route="Home"
        ContentTemplate="{DataTemplate view:Home}"/>

        <ShellContent
        x:Name="queryPage"  
        Title="Query"
        Route="Query" 
        ContentTemplate="{DataTemplate view:Query}"/>
    </FlyoutItem>-->

    <TabBar>
        <Tab Title="Main">
            <ShellContent
                x:Name="homePage"    
                Title="Home"
                Route="Home"
                ContentTemplate="{DataTemplate view:Home}"/>

            <ShellContent
                Title="Settings"
                Route="Settings" 
                ContentTemplate="{DataTemplate view:Settings}"/>
        </Tab>
    </TabBar>
</Shell>
